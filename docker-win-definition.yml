name: com.newrelic.docker
description: Reports status and metrics for nri-docker service
protocol_version: 1
os: windows

commands:
  all_data:
    command:
      - ./bin/nri-docker
    interval: 15

